#include <stdio.h>

int main() {
    int n, i, j, count, printed;

    printf("Enter number of elements: ");
    scanf("%d", &n);

    int a[n];
    printf("Enter the elements:\n");
    for (i = 0; i < n; i++)
        scanf("%d", &a[i]);

    printf("Elements repeated exactly twice: ");
    printed = 0;

    for (i = 0; i < n; i++) {
        count = 1;
        int alreadyCounted = 0;

        for (j = 0; j < i; j++) {
            if (a[i] =
