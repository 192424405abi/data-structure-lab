#include <stdio.h>
#include <string.h>

int main() {
    char s[200];
    int freq[256] = {0};
    int i;

    printf("Enter a string: ");
    scanf("%199s", s);

    for (i = 0; s[i] != '\0'; i++) {
        freq[(unsigned char)s[i]]++;
    }

    printf("Repeated characters: ");
    int found = 0;

    for (i = 0; i < 256; i++) {
        if (freq[i] > 1) {
            printf("%c ", i);
            f
